Open a `.y` or `.yacc` grammar file to start the Collie language server.

Once active, Collie contributes syntax highlighting, document diagnostics, formatting, fix-all actions, symbol search, definition lookup, references, rename, folding, and syntax diagram previews when the server supports them.

Use [Collie: Show Output Channel](command:collie.showOutputChannel) if setup or server startup needs inspection.
